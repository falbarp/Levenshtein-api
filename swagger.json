{
    "swagger": "2.0",
    "info": {
      "title": "Levenshtein Distance",
      "description": "API for Levenshtein Distance calculation",
      "version": "1.0"
    },
    "host": "localhost:8001",
    "basePath": "/",
    "schemes": [
      "http"
    ],
    "paths": {
      "/distance/{a}&{b}": {
        "get": {
          "description": "Enter two words",
          "operationId": "ADD",
          "parameters": [
            {
              "name": "a",
              "in": "path",
              "description": "First word.",
              "required": true,
              "default": "example",
              "enum": [
                "example"
              ]
            },
            {
              "name": "b",
              "in": "path",
              "description": "First word.",
              "required": true,
              "default": "exemplo",
              "enum": [
                "Exemplo"
              ]
            }
          ],
          "responses": {}
        }
      }     

    }   
}